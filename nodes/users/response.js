module.exports = {
    getResponseContent: function(type, data) {
    
        // var content = Object.assign({}, baseResponse[type]);
        
        // if(data) {
        //     content.user = data;
        //     content.online = true;
        // }

        // return  content;
    }
}